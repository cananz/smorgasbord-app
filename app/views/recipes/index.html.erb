<h2>Recipes:</h2>

<ul>
  <% @recipes.each do |r| %>
    <li><%= link_to r[:recipe][:name], recipe_path(r[:recipe]) %> &ensp;||&ensp; You're missing <%= pluralize(r[:missing], 'ingredient') %>.</li>
  <% end %>
</ul>

<%= link_to "Head back to the kitchen", user_path(current_user) %>