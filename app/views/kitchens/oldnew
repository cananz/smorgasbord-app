<%= form_for @kitchen do |f| %>
  <%= f.collection_check_boxes(:ingredient_id, Ingredient.all, :id, :name) do |ing| %>
    <div>
      <%= ing.check_box %><%= ing.label { ing.text } %>
    </div>
  <% end %>
  <br>
  <%= f.submit %>
<% end %>